```jsx
name = "mike";
age = 30;
```

;(세미클론)은 한 줄이 끝났다는 의미로 적고 생략해도 되지만 항상 써주는 것이 좋음

mike는 따옴표로 감싸져 있는데 문자열은 항상 따옴표로 감싸줘야 함 큰따옴표 , 작은따옴표 상관없이

calss = “수업” 같은 코드는 에러가 남 클래스는 js에서 사용하는 이름이기 때문에 = 예약어

alert() 함수 = 경고창

 

console.log() = 로고를 찍는 함수

name이란 변수를 짓는건 위험 

let name = “mike”;  바꿀 수 있는 변수 let

const = 상수(대문자로 선언하는 것이 좋음)

- 변수는 문자와 숫자 , $ , _ 만 사용 가능
- 첫 글자는 숫자가 안됨
- 예약어는 사용 불가
- 가급적 상수는 대문자로
- 변수명은 읽기 쉽고 이해할 수 있게 선언